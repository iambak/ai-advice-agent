# AI Advice Agent Environment Configuration
# Copy this file to .env and update the values as needed

# Permission API Configuration
PERMISSION_API_URL=https://kpfnbcvnfb.execute-api.us-east-1.amazonaws.com/dev

# Agent Configuration
AGENT_NAME=advice-agent

# Logging Configuration
LOG_LEVEL=INFO

# Development Configuration
ENVIRONMENT=development

# Optional: Custom timeouts for API requests (in seconds)
API_TIMEOUT=10

# Optional: Rate limiting (requests per minute)
RATE_LIMIT=60